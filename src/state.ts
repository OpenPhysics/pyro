/** Centralized application state. Importing this module gives a single place
 *  to inspect all runtime state when debugging. */
export const appState = {
  isRunning: false,
  currentFontSize: 14,
  isFullScreen: false,
  isSidebarCollapsed: true,
};
